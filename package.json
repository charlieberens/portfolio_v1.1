{
    "name": "portfolio_v1.1",
    "version": "1.1.0",
    "description": "",
    "main": "app.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node app.js",
        "portfolio-dev": "concurrently \"nodemon server.js\" \"npm --prefix ./projects/portfolio run start\"",
        "build": "cd projects/portfolio && (npm install && npm run build)"
    },
    "author": "Charlie Berens",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^7.0.0",
        "dotenv": "^16.0.0",
        "express": "^4.17.2",
        "nodemailer": "^6.7.2"
    }
}
